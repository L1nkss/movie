export enum ButtonTypes {
    'default' = 'default',
    'primary' = 'primary',
    'link' = 'link',
    'text' = 'text',
}

export enum ButtonHtmlTypes {
    'submit' = 'submit',
    'button' = 'button',
    'reset' = 'reset'
}